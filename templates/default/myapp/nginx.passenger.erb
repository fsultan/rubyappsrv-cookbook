server {
  listen   80;
  server_name  <%= @hostname %>;

  access_log  <%= @log_dir %>/localhost.access.log;

  location / {
    passenger_enabled on;
    rails_env "<%= @rails_env %>";
    root   <%= @app_dir %>;
    index  index.html index.htm;
  }
}
